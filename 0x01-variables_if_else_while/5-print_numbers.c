#include <stdio.h>
/**
 * main - Program that prints all single digit numb of base 10 startn from 0
 * Return: Always 0
*/
int main(void)
{
int num;
for (num = 0; num < 10; num++)
	printf("%d", num);
printf('\n');
return (0);
}
